# Modelos locales

Los archivos ONNX no se guardan en Git. Antes de construir la imagen copia aquí:

- `bg-remove.onnx`
- `realesr-anime-x4.onnx`

Ambos están en `Builder-current/models/` y son los mismos modelos usados por el servicio actual.

